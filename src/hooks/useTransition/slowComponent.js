import React from 'react'
import {View, TouchableOpacity, Text} from 'react-native'

const SlowComponent = ({value}) => {
    return (
        <View>
            <Text style= {{marginBottom:10, fontWeight: "bold"}}>useTransition</Text>
             
        </View>
    )
}
export default SlowComponent
